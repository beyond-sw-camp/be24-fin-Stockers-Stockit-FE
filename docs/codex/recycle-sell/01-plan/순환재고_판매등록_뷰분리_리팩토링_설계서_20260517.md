# 순환재고 판매등록 뷰 분리 리팩토링 설계서

## 1. 문서 개요
- 작성일: 2026-05-17
- 대상 워크스페이스: `C:\Users\saral\Desktop\Stockit`
- 대상 범위:
  - `Stockit-FE/src/views/hq/circular-stock/HqCircularStockSalesRegisterView.vue`
  - `Stockit-FE/src/views/hq/circular-stock/HqCircularStockSalesSkuSelectView.vue`
  - 필요 시 연관 스토어/공용 컴포넌트(`circularStock.js`, `CircularStockInventoryBrowseSection.vue`)
- 목표:
  - 기능/동작/문구 의미를 바꾸지 않고 컴포넌트 분리
  - 미사용 코드 정리
  - 유지보수성 향상

## 2. 절대 준수 조건
- 동작, 비즈니스 로직, 검증 결과, 문구 의미 변경 금지
- 한글 깨짐 금지
- 기존 사용자 변경사항 되돌리지 않음
- 파괴적 명령 금지 (`git reset --hard` 등)
- UI 개선이 아니라 구조 개선 중심
- 빌드 통과 및 회귀 검증 필수

## 3. 현황 분석 요약

### 3.1 RegisterView 역할 혼재
- 단일 파일 내에 아래 역할이 결합되어 있음
  - 레이아웃/헤더
  - 스텝퍼
  - Step1(SKU 확인)
  - Step2(거래처 매칭: AI 추천/수동 선택)
  - Step3(판매 조건 입력, 자동 배분/클램프)
  - 하단 스텝별 액션 바
  - 최종 확인 모달
- 2000줄 이상 대형 파일로 변경 영향 범위가 넓고 회귀 위험이 큼

### 3.2 SkuSelectView 역할 혼재
- 조회 섹션, 하단 고정 바, 선택 SKU 모달, 조건 변경 확인 모달이 단일 뷰에 결합

### 3.3 미사용 코드 후보(구현 시 재확인 후 제거)
- RegisterView 후보
  - computed/state: `shouldRenderDrawer`, `browseSummaryText`, `draftRowIds`, `step3HasOverLimit`, `matchingMaterialBadges`, `matchingContextSummary`
  - function: `openDrawer`, `toggleDrawer`, `toggleStep3Detail`, `isStep3DetailOpen`, `isPriceEditMode`, `openPriceEditMode`, `closePriceEditMode`
  - import 후보: `CircularStockInventoryBrowseSection`
- SkuSelectView 후보
  - function: `moveToWorkflow`

## 4. 분리 설계

## 4.1 제안 파일 구조
- `Stockit-FE/src/components/hq/circular-stock/sales-register/`
  - `SalesRegisterStepper.vue`
  - `SalesRegisterStep1SkuTable.vue`
  - `SalesRegisterStep2BuyerSection.vue`
  - `SalesRegisterStep3ConditionsSection.vue`
  - `SalesRegisterStepFooter.vue`
  - `SalesRegisterFinalReviewModal.vue`
- `Stockit-FE/src/components/hq/circular-stock/sales-sku-select/`
  - `SalesSkuSelectBottomBar.vue`
  - `SalesSkuSelectSelectedSkuModal.vue`
  - `SalesSkuSelectResetConfirmModal.vue`

## 4.2 Props/Emits 계약 원칙
- 하위 컴포넌트는 표시/입력/이벤트 전달 역할만 수행
- 스토어 접근, 라우팅, 최종 submit, step 이동 가드는 상위 뷰에서 유지
- 이벤트명은 기존 핸들러 의도를 드러내도록 명확히 정의

## 4.3 상태 소유권
- 상위(뷰) 유지:
  - `saleStep`, `draftItems`, `selectedBuyer`, `submitValidation` 등 핵심 워크플로 상태
  - 모달 open/close 상태
  - Step 이동 가드, submit 로직, 추천 fetch 로직
- 하위(컴포넌트) 이관 가능:
  - 단순 UI 상태(탭 표현, 카드 펼침 표현 등)
  - 단, 1차 리팩토링에서는 회귀 리스크 최소화를 위해 Step3 입력 연산 상태는 상위 유지

## 5. 단계별 실행 계획 (안전 우선)

### 1단계: 사전 안전정리
- 분리 전 동작 체크포인트 고정
- 미사용 코드 후보 재검증
- 산출물: 기준 동작 체크리스트

### 2단계: RegisterView 1차 분리(저위험)
- `Stepper`, `Step1`, `StepFooter` 분리
- 로직은 부모 유지
- 검증: 빌드 + Step 이동/버튼 활성

### 3단계: RegisterView 모달 분리
- `FinalReviewModal` 분리
- 검증: 빌드 + 모달 진입/닫기/복귀/최종등록 흐름

### 4단계: RegisterView Step2 분리
- `Step2BuyerSection` 분리
- 검증: 빌드 + 거래처 선택/복귀/선행조건 가드

### 5단계: RegisterView Step3 분리
- `Step3ConditionsSection` 분리
- 검증: 빌드 + 입력/클램프/토글/상태문구/버튼 활성

### 6단계: SkuSelectView 분리 + 정리
- 하단바/모달 3종 분리
- 미사용 코드 최종 제거
- 검증: 최종 빌드 + 전체 회귀 체크

## 6. 회귀 검증 기준
- SKU 선택/해제
- 선행조건 가드(창고/소재)
- Step 이동 가드
- Step2 거래처 선택/복귀
- Step3 입력/클램프/토글
- 하단 상태 문구/버튼 활성 정합성
- 최종 확인 모달 진입

## 7. 리스크 및 대응
- 리스크: 이벤트 계약 누락으로 동작 불일치
  - 대응: 분리 단위별 props/emits 체크리스트 사용
- 리스크: Step3 계산 흐름 변경
  - 대응: Step3 계산/검증 로직은 상위 유지 후 점진 분리
- 리스크: 한글 문자열 변형/깨짐
  - 대응: 문자열 원문 유지, 변경 최소화

## 8. 산출물 정의
- 변경 파일 목록
- 분리 컴포넌트 목록 및 역할
- 삭제된 미사용 코드 목록
- 빌드 결과
- 회귀 검증 결과
- 남은 TODO(있는 경우)

## 9. 완료 기준 (Definition of Done)
- `npm run build` 성공
- 회귀 검증 항목 전부 통과
- 기능/문구/검증 결과 동일성 확인
- 리팩토링 결과 보고서 제출
